{
    "locals": [
        {
            "comon_tags": {
                "billing_code": "${var.billing_code}",
                "company": "${var.company}",
                "project": "${var.company}-${var.project}"
            }
        }
    ]
}